[tox]
envlist =
    py{35,36,37,38}
    pypy3
    cov

[testenv]
deps =
    pytest

commands =
    python setup.py test

[testenv:cov]
deps =
    .[test]
    coverage[toml]
    pytest-cov
commands =
    python setup.py test --addopts "-vv --cov"
